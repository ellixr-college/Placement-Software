auto-install-peers=true
strict-peer-dependencies=false
# Flat node_modules (npm-style). Required so the bundled API can resolve
# transitive runtime deps like @prisma/client, and so Prisma's generated client
# resolves correctly (pnpm's symlinked layout breaks both on deploy).
node-linker=hoisted
